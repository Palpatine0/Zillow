server:
  port: 9006
spring:
  application:
    name: zillow-buytime
  profiles:
    active: itemCacheName,mongodb,redis
  cloud:
    config:
      uri: http://localhost:3355
      label: master
      name: zillow
      profile: dev