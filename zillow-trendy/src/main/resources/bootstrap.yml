server:
  port: 9001
spring:
  application:
    name: zillow-trendy
  profiles:
    active: mongodb,bannerNginx
  cloud:
    config:
      uri: http://localhost:9010
      label: master
      name: zillow
      profile: dev