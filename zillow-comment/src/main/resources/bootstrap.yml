server:
  port: 9005

spring:
  application:
    name: zillow-comment
  profiles:
    active: mongodb
  cloud:
    config:
      uri: http://zillow-config-server:3355
      label: master
      name: zillow
      profile: prd
